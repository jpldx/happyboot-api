spring:
  # 环境 dev:开发环境|test:测试环境|prod:生产环境
  profiles:
    active: dev
  servlet:
    # 上传文件大小请求拦截限制
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

logging:
  config: classpath:logback-spring.xml